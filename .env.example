# Environment Configuration for Flips & Bidz Backend

# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Connection
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/hibid

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/hibid?retryWrites=true&w=majority

# Gmail API Configuration (OAuth2)
# Follow GMAIL_API_SETUP.md for detailed instructions
# Get credentials from: https://console.cloud.google.com/
GMAIL_USER=flipsnbidz@gmail.com
GMAIL_CLIENT_ID=your_client_id_here.apps.googleusercontent.com
GMAIL_CLIENT_SECRET=your_client_secret_here
GMAIL_REDIRECT_URI=https://developers.google.com/oauthplayground
GMAIL_REFRESH_TOKEN=your_refresh_token_here

# INSTRUCTIONS:
# 1. Copy this file and rename it to .env (remove .example)
# 2. Fill in your actual MongoDB URI
# 3. Set up Gmail App Password and add it to EMAIL_PASS
# 4. Never commit the .env file to version control (it's in .gitignore)
